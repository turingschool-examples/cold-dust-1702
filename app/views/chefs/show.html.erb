<h1>Chef Show Page</h1>
<br>
<p>Chef Name: <%= @chef.name %></p>
<p><%= link_to "View This Chef's Ingredients", "/chefs/#{@chef.id}/ingredients" %></p>
<br>

<p>This Chef's Top 3 Most Used Ingredients:</p>
<% @chef.top_3_ingredients.each_with_index do |ingredient, index| %>
  <p><%= "#{index + 1}. #{ingredient.name}" %></p>
<% end %>
